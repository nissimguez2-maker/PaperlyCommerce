# Netlify configuration for Paperly (Next.js App Router).
[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

# Official Next.js runtime — handles SSR, route handlers, image optimization.
[[plugins]]
  package = "@netlify/plugin-nextjs"
